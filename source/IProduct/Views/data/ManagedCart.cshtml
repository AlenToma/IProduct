@using IProduct.Models;


@Html.JSX("shoppingCart = ShoppingCart", new
    {
        get = @Url.Action("GetCart", "Home", null, Request.Url.Scheme),
        save = @Url.Action("AddCart", "Home", null, Request.Url.Scheme),
        field = @Url.Action("UpdateField", "Home"),
        connector = ".userData a .fa-shopping-cart",
        image = @Url.Action("index", "Home", null, Request.Url.Scheme),
        display = "block"
    })



<script>
    @*$(document).ready(function () {


        shoppingCart = $(".crt").shoppingCart({
            get: '@Url.Action("GetCart", "Home")',
            save: '@Url.Action("AddCart", "Home")',
            field: '@Url.Action("UpdateField", "Home")',
            connector: $(".veryTop > .cart"),
            image: '@Url.Action("index", "Home", null, Request.Url.Scheme)',
            display: "block"
        });

        var data = ReactDOM.render(React.createElement(ShoppingCart,
            {
                get: '@Url.Action("GetCart", "Home")',
                save: '@Url.Action("AddCart", "Home")',
                connector: "",
                image: '@Url.Action("Index", "Home", null, Request.Url.Scheme)',
                display: "none"
            }),
            document.getElementById('content'));
    });*@
</script>