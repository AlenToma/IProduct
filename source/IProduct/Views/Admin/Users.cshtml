
@{
    ViewBag.Title = "Users";
    Layout = "~/Views/Admin/Shared/_AdminLayout.cshtml";
}

<script>
    var tabContainer;
    $(document).ready(function () {
        tabContainer = $(".centercontent").tabs({
            onSelect: function (tab) {
                if (tab.onSelect)
                    tab.onSelect(tab);
            }
        });
        var products = tabContainer.add("", "Users", "Users").select();
        Partial("data/Users.cshtml", function (data) { products.content.append(data) });

        var categoriees = tabContainer.add("", "Roles", "Roles");
        //Partial("data/Categories.cshtml", function (data) { categoriees.content.append(data) });

    });
</script>